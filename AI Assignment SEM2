{
  "cells": [
    {
      "cell_type": "markdown",
      "metadata": {
        "id": "view-in-github",
        "colab_type": "text"
      },
      "source": [
        "<a href=\"https://colab.research.google.com/github/sangitaGIT011/Myproj/blob/main/AI%20Assignment%20SEM2\" target=\"_parent\"><img src=\"https://colab.research.google.com/assets/colab-badge.svg\" alt=\"Open In Colab\"/></a>"
      ]
    },
    {
      "cell_type": "code",
      "source": [
        "# Install Fairlearn in Colab\n",
        "!pip install fairlearn --quiet\n",
        "\n",
        "import numpy as np\n",
        "import pandas as pd\n",
        "from sklearn.ensemble import RandomForestClassifier\n",
        "from sklearn.model_selection import train_test_split\n",
        "from sklearn.metrics import accuracy_score\n",
        "from fairlearn.metrics import selection_rate, MetricFrame, demographic_parity_difference\n",
        "from fairlearn.reductions import ExponentiatedGradient, DemographicParity\n",
        "\n",
        "# --- 1. Create tiny biased dataset ---\n",
        "np.random.seed(42)\n",
        "n = 200\n",
        "gender = np.random.choice(['Male','Female'], size=n, p=[0.7,0.3])\n",
        "experience = np.random.randint(0,10, size=n)\n",
        "education = np.random.randint(0,3, size=n)\n",
        "y_true = ((experience + education) > 5).astype(int)\n",
        "y_biased = np.where(gender=='Female', 0, y_true)  # bias against females\n",
        "\n",
        "X = pd.DataFrame({'experience': experience, 'education': education})\n",
        "y = y_biased\n",
        "A = gender  # protected attribute\n",
        "\n",
        "# --- 2. Train/test split ---\n",
        "X_train, X_test, y_train, y_test, A_train, A_test = train_test_split(X, y, A, test_size=0.3, random_state=42)\n",
        "\n",
        "# --- 3. Train black-box model ---\n",
        "clf = RandomForestClassifier(n_estimators=50, random_state=42)\n",
        "clf.fit(X_train, y_train)\n",
        "y_pred = clf.predict(X_test)\n",
        "\n",
        "# --- 4. Evaluate accuracy & fairness ---\n",
        "print(f\"Accuracy: {accuracy_score(y_test, y_pred):.2f}\")\n",
        "\n",
        "metric_frame = MetricFrame(metrics=selection_rate, y_true=y_test, y_pred=y_pred, sensitive_features=A_test)\n",
        "print(\"Selection rates by gender:\\n\", metric_frame.by_group)\n",
        "\n",
        "dp_diff = demographic_parity_difference(y_test, y_pred, sensitive_features=A_test)\n",
        "print(f\"Demographic Parity Difference (before mitigation): {dp_diff:.2f}\")\n",
        "\n",
        "# --- 5. Mitigate bias ---\n",
        "mitigator = ExponentiatedGradient(clf, constraints=DemographicParity())\n",
        "mitigator.fit(X_train, y_train, sensitive_features=A_train)\n",
        "y_pred_fair = mitigator.predict(X_test)\n",
        "\n",
        "dp_diff_fair = demographic_parity_difference(y_test, y_pred_fair, sensitive_features=A_test)\n",
        "print(f\"Demographic Parity Difference (after mitigation): {dp_diff_fair:.2f}\")"
      ],
      "metadata": {
        "id": "AZzHwBu7BzkB",
        "outputId": "a28b55ee-cbb1-4cb2-ae1b-cc01431da971",
        "colab": {
          "base_uri": "https://localhost:8080/"
        }
      },
      "execution_count": 3,
      "outputs": [
        {
          "output_type": "stream",
          "name": "stdout",
          "text": [
            "Accuracy: 0.73\n",
            "Selection rates by gender:\n",
            " sensitive_feature_0\n",
            "Female    0.277778\n",
            "Male      0.238095\n",
            "Name: selection_rate, dtype: float64\n",
            "Demographic Parity Difference (before mitigation): 0.04\n",
            "Demographic Parity Difference (after mitigation): 0.06\n"
          ]
        }
      ]
    },
    {
      "cell_type": "code",
      "source": [
        "# Install Fairlearn\n",
        "!pip install fairlearn --quiet\n",
        "\n",
        "import pandas as pd\n",
        "from sklearn.ensemble import RandomForestClassifier\n",
        "from sklearn.model_selection import train_test_split\n",
        "from sklearn.metrics import accuracy_score\n",
        "from fairlearn.metrics import selection_rate, MetricFrame, demographic_parity_difference\n",
        "from fairlearn.reductions import ExponentiatedGradient, DemographicParity\n",
        "\n",
        "# --- 1. Load dataset from GitHub ---\n",
        "url = \"https://raw.githubusercontent.com/sangitaGIT011/Myproj/refs/heads/main/recruitment_dataset.csv\"  # replace with your GitHub raw CSV URL\n",
        "df = pd.read_csv(url)\n",
        "\n",
        "# Check dataset\n",
        "#print(df.head())\n",
        "\n",
        "# --- 2. Prepare features and target ---\n",
        "X = df[['experience','education_score']]\n",
        "y = df['selected']\n",
        "A = df['gender']  # protected attribute\n",
        "\n",
        "# --- 3. Train/test split ---\n",
        "X_train, X_test, y_train, y_test, A_train, A_test = train_test_split(X, y, A, test_size=0.3, random_state=42)\n",
        "\n",
        "# --- 4. Train black-box model ---\n",
        "clf = RandomForestClassifier(n_estimators=50, random_state=42)\n",
        "clf.fit(X_train, y_train)\n",
        "y_pred = clf.predict(X_test)\n",
        "\n",
        "# --- 5. Evaluate accuracy & fairness ---\n",
        "print(f\"Accuracy: {accuracy_score(y_test, y_pred):.2f}\")\n",
        "\n",
        "metric_frame = MetricFrame(metrics=selection_rate, y_true=y_test, y_pred=y_pred, sensitive_features=A_test)\n",
        "print(\"Selection rates by gender:\\n\", metric_frame.by_group)\n",
        "\n",
        "dp_diff = demographic_parity_difference(y_test, y_pred, sensitive_features=A_test)\n",
        "print(f\"Demographic Parity Difference (before mitigation): {dp_diff:.2f}\")\n",
        "\n",
        "# --- 6. Mitigate bias ---\n",
        "mitigator = ExponentiatedGradient(clf, constraints=DemographicParity())\n",
        "mitigator.fit(X_train, y_train, sensitive_features=A_train)\n",
        "y_pred_fair = mitigator.predict(X_test)\n",
        "\n",
        "dp_diff_fair = demographic_parity_difference(y_test, y_pred_fair, sensitive_features=A_test)\n",
        "print(f\"Demographic Parity Difference (after mitigation): {dp_diff_fair:.2f}\")"
      ],
      "metadata": {
        "id": "cCEl7T6TFugX",
        "outputId": "04189a67-1053-4df7-fbab-22b4223251dd",
        "colab": {
          "base_uri": "https://localhost:8080/"
        }
      },
      "execution_count": 7,
      "outputs": [
        {
          "output_type": "stream",
          "name": "stdout",
          "text": [
            "Accuracy: 0.67\n",
            "Selection rates by gender:\n",
            " gender\n",
            "Female    0.0\n",
            "Male      0.5\n",
            "Name: selection_rate, dtype: float64\n",
            "Demographic Parity Difference (before mitigation): 0.50\n",
            "Demographic Parity Difference (after mitigation): 0.00\n"
          ]
        }
      ]
    }
  ],
  "metadata": {
    "colab": {
      "name": "Welcome to Colab",
      "toc_visible": true,
      "provenance": [],
      "include_colab_link": true
    },
    "kernelspec": {
      "display_name": "Python 3",
      "name": "python3"
    }
  },
  "nbformat": 4,
  "nbformat_minor": 0
}